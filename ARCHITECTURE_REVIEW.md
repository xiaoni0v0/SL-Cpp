# runtime/ 设计说明（给人工 code review 用）

这份文档只讲 `runtime/` 这一整块——对象模型、GC、bootstrap、异常。目标是让你不用逐文件翻代码，
先在脑子里搭好整体骨架，再带着骨架去看具体实现。

**整体定位**：`runtime/` 是 SL 的对象系统，独立于编译器和虚拟机存在。编译器（`compiler/`）以后会
直接调用这里的类型来构造常量；虚拟机（还没写）会调用这里的类型来执行代码。这一层现在完全不认识
"SL 源码""字节码"这些概念，只管"SL 的值长什么样、怎么活、怎么死"。

---

## 一、对象模型：`Object` / `Type` / `Ref<T>`

### `Object`（`Object.h`）

一切 SL 对象的共同基类。每个 `Object` 有：

- **引用计数** `refcount_`——单线程整数，不是原子操作；
- **所属类型** `type_`（一个 `Ref<Type>`，强引用）；
- **GC 用的两个链表指针 + 一个标记位**（下面「GC」一节讲）。

`Object` 本身是抽象类：它有一个纯虚函数 `visit_own_refs()`，逼着每个具体子类回答"我自己（不算
`type_`）还强引用着谁"。比如 `Tuple` 要报出它装的每个元素，`Int` 因为只存一个 `BigInt`（不是别的
SL 对象），报空。**这个函数是新增字段时最容易漏改的地方**——漏了不会编译报错，只会让 GC 把这个对象
指向的东西误判成"没人引用了"提前回收。

**关键设计判断：C++ 类继承 ≠ SL 类继承。**
C++ 这边的类只表达"存储形状"——这个对象有哪些字段、多大、怎么析构。SL 那边的继承关系
（`int` 继承 `numbers.Real` 之类）完全记在 `Type` 对象自己的字段里，两套体系不挂钩。
所以：

- `Bool`（True/False）不是 `Int` 的 C++ 子类，尽管两者看着都是"数字"；
- `numbers.Real` 这种在 SL 里存在、但没人会直接实例化的抽象类，压根没有对应的 C++ 类，
  它就是一个 `Type` 对象，没有 `Object` 子类挂在它下面。

### `Type`（`Type.h`）

类型对象本身。装着：

- `name_`：SL 层的类名（比如 `"int"`、`"decimal.DecimalException"`）；
- `bases_`：直接基类列表（强引用）；
- `mro_`：方法解析顺序，**存裸指针**，不是强引用。

`mro_` 为什么不强引用？因为 `mro_[0]` 就是自己——如果这也是强引用，每个类型对象都会自己指向自己，
形成一个引用计数永远清不掉的环。它之所以能安全用裸指针，是因为 `mro_` 里除了自己之外的每一项，
都必然也出现在 `bases_` 能链到的地方，本来就被强引用着，GC 顺着 `bases_` 也能扫到它们。

**限制：现在只支持单继承**，多基类会在构造时直接抛 `InternalError`（C3 线性化算法还没写，SL 目前
也没有能让用户定义多继承类的语法，用不上）。

### `Ref<T>`（侵入式引用计数句柄，`Object.h` 里）

这是整个对象模型里最常打交道的东西——凡是"持有一个 SL 对象"，用的都是 `Ref<T>`，从不用裸指针
持有所有权。规则很简单也很死：**构造就 +1，析构就 -1**，没有第二种入口。好处是不会出现"这个指针
到底算不算我的"这种歧义。

裸指针（`Object*`、`Type*`）在这套代码里的含义是**借用**——只是"看一眼"，不代表你要为它的生死
负责。`Runtime` 的所有访问器（下面会讲）返回的都是裸指针，因为运行时活着的时候这些东西保证一直
活着，用不着每次都包一层。

为什么不用标准库的 `shared_ptr`？因为 GC 需要能读、能改这个计数（分离垃圾之后要能把计数临时抬高，
见下面「GC」一节的第 3 步），`shared_ptr` 的计数被封死在控制块里，够不着。

所有具体对象类型的**构造函数也是私有的**，只对 `make_ref` 开放（`SL_HEAP_ONLY` 宏）。于是
`Int x{...};` 这种栈上构造、以及 `new Int{...}` 之后忘了包 `Ref`，全都是编译错误——前者会在
引用计数归零时对栈内存 `delete this`，后者必然泄漏。

加减计数是 `Object` 的**私有成员函数** `incref()`/`decref()`，`decref()` 归零时 `delete this`。
只有 `Ref<T>` 和 `Heap` 是友元能调它们——也就是说"手动改引用计数"这件事在 `runtime/` 之外根本
写不出来，是编译错误而不是靠自觉。（Boost 的 `intrusive_ptr` 用自由函数是为了让任意类型都能接入，
这里 `Ref` 只服务 `Object` 一族，不需要那份通用性，换来的是更紧的封装。）

---

## 二、GC：`Heap`

SL 语言规范定的策略是"引用计数为主，堆扫描处理循环引用"——`Ref<T>` 就是引用计数那一半，
**绝大多数对象在最后一个 `Ref` 析构时就直接释放**，根本轮不到 GC 出手。`Heap::collect()`
只处理引用计数解决不了的那部分：**环**（比如两个对象互相强引用，谁的计数都降不到 0）。

### 怎么找到环：标记-清扫，而不是"引用计数试探法"

CPython 用的是另一种做法（trial deletion：把每个对象内部的引用数减掉，剩下的部分如果还大于 0
就说明有"外部"引用它）。之所以没抄它，是因为它是 CPython 在"没法枚举所有根"这个客观限制下的
无奈选择——C 层的调用栈上到处是裸引用，没法把它们都找出来当作根。

这份代码不存在这个限制：SL 的执行状态被设计成**全部长在堆对象上**（帧对象、常量表等），
C++ 调用栈本身不应该持有长期存活的引用。所以能明确枚举出"谁是根"，也就能直接用更朴素、更好验证
正确性的标记-清扫：从根出发，能走到的都标记为存活，扫一遍全堆，没被标记的就是垃圾。

**这决定了一条硬规矩**：`collect()` 只能在"C++ 调用栈上没有任何存活 `Ref`"的时刻调用，
不然栈上那些没被算进根集合的 `Ref` 指向的对象会被误杀。这就是为什么虚拟机以后要在
"两条指令之间"这种干净的时间点才允许触发 GC，而不是随便什么时候都能收。

### 谁是根：`GcRootSource`

一个小接口，谁想让自己攥着的对象被算进根集合，就实现它、注册进 `Heap`。现在只有 `Runtime`
一个实现（它攥着全部内置类型和单例）。以后帧栈、模块表之类会陆续加入。

### `collect()` 的五步

1. **标记**：从每个根出发，把能走到的对象全部标记；
2. **分离**：扫一遍全堆链表，没被标记的收进"垃圾"列表（顺便把标记复位，省得再扫一遍去复位）；
3. **临时保命**：给每个垃圾对象的引用计数 +1；
4. **断边**：让每个垃圾对象放弃它自己持有的引用（`visit_own_refs` 又被复用了一次，
   这次访问者的动作是清空槽位而不是只读）；
5. **真正释放**：把第 3 步加的 +1 减回去，此时该归零的就都归零了，正常触发析构。

**第 3 步为什么必须存在**：如果不这么做，直接进入第 4 步断边，垃圾 A 断开对垃圾 B 的引用时，
B 的计数可能直接归零、立刻被析构——但 B 可能马上又要在第 4 步里对 A "断边"，这时候 A 早被清理过，
是典型的 use-after-free。先集体 +1 把"死亡时刻"统一推迟到第 5 步，就不用管断边的顺序了。

### 全堆链表 vs. 分代/写屏障之类的优化

现在完全没做任何优化——一个侵入式双向链表串起全部存活对象，`collect()` 每次都扫全堆。
这是刻意的：这些优化的价值要等虚拟机跑起来、能实测出"到底哪里慢"之后再谈，现在加只是
凭空猜测收益、徒增复杂度。

---

## 三、`Runtime`：全局单例 + 分阶段启动

### 为什么是全局单例

SL 没有"多解释器"这种概念——`import`、`eval_isolated` 建的是新的**全局作用域**，不是新的运行时。
一个进程里从头到尾只有一份对象类型系统，做成通过静态函数访问的单例，比把一个 `Runtime&`
参数穿透传进每个对象构造函数、每条虚拟机指令要干净得多。

### bootstrap 没有分阶段的状态机——这条是审出来改掉的，记一下过程

`init()` 分两步：`build_types()` 建全部内置类型对象，`build_singletons()` 建六个单例。**曾经**
给这两步之间配过一个有序枚举 `BootPhase`（`Uninitialized`→`Types`→`Values`→`Ready`），每个访问器
声明自己要求的最低相位，理由是"防止提前访问了还没建好的东西"。

审这块代码时被问到一个很直接的问题：这条防线拦的到底是谁？逐个查了全部 `instance(BootPhase::X)`
的调用点，发现**没有一条来自真正意义上的外部代码**——全部来自 `Runtime` 自己生成的访问器。往下
追，会发现"需要"区分这两个状态的根源是自己造出来的：`build_singletons()` 当时调用 `type_none_type()`
去拿类型，`Bool` 的构造函数当时硬编码调用 `Runtime::type_bool()`——都是绕道公开的、带检查的静态
访问器，而不是直接读 `Runtime` 自己的私有字段，或者由调用方直接把 `Type*` 传进去。C++ 单线程
同步执行，`init()` 跑到一半时不存在任何别的代码能观察到"类型建好、单例没建好"这个中间状态——它
只在 `init()` 那一次函数调用内部瞬间存在。**区分它俩的检查是自己在跟自己的实现细节较劲**：检查
存在的唯一理由是"有代码会被这个检查拦下"，而那些代码正是绕出去又绕回来的自己。

修法：`build_singletons()` 改成直接读 `types_[...]` 私有字段；`Bool` 的构造函数改成跟
`NamedSingleton`/`BaseException` 一样接收调用方传来的 `Type*`。改完之后两个状态在任何地方都不再需要区分，
整个 `BootPhase` 枚举删掉，`Runtime::instance()` 现在只剩最初就该有的那一条检查：`g_runtime`
是否为空——也就是"`init()` 到底跑没跑完"这一个二元状态，用一个空指针检查就够。`ready()` 相应地
变成 `g_runtime != nullptr`。

这里想强调的不是"我们删了个枚举"，是审查这类状态机设计时该问的问题：**这个状态谁在外部会真的
观察到、谁会真的需要区分**，而不是"这个状态在概念上是不是存在"——`Types`/`Values` 概念上确实是
两个不同的完成度，但概念上存在不代表有任何代码合法地依赖这个区分。判断方法很直接：把所有读这个
状态的调用点列出来，看它们是不是都是自己写的、还绕得回去的内部实现。

### 内置类型清单：`x_builtin_types.inc`

这是个 X-Macro 文件，每一行 `X(枚举名, 访问器名, SL类名, 基类)` 同时驱动三处代码：
`BuiltinType` 枚举、`Runtime` 上一批同名访问器（`type_int()`、`type_syntax_error()` 等）、
以及 bootstrap 时真正建这些类型对象的循环。新增一个内置类型只需要在这个文件里加一行，
不用在三个地方分别改。

内容分两段：前段是基础类型（`object`/`type`/`int`/`str`/`tuple`/`decimal`/`bool` 等），
后段是照抄 SL.md 规范里那张异常类继承图建出来的整棵异常类树（`BaseException` 往下到
`TypeError`/`ValueError`/`SyntaxError` 等十几个）。**这些异常类只是空壳"类型对象"，还没有
挂任何行为**——现在只解决了"这些类存在、继承关系对不对"，方法（比如以后可能有的 `__str__`）
还没有实现的地方。

### 初始化/关闭：`init()` / `shutdown()`

`init()` 如果中途某一步失败（比如某个类型构造函数抛异常），会把已经建了一半的东西全部拆掉再
把异常重新抛出去——不允许留下一个"半初始化"的运行时挂在那里，这种状态比"压根没初始化"更危险，
因为它看起来能用，实际上残缺不全。

`shutdown()` 有个不直观的地方：内置类型之间存在一个引用计数天生解不开的环——每个类型对象都
强引用着"type"这个元类，而"type"自己的元类又是它自己。这个环只能靠上面说的 GC 来破，
所以 `shutdown()` 的最后一步是先放弃 `Runtime` 自己持有的全部引用、把自己从根源列表里摘掉，
然后手动触发一次 `collect()`——此时没有任何根了，整个类型系统会被当作垃圾一次性清空。

---

## 四、`runtime/objects/`：具体的值类型

这些都是"内置类型的存储实现"，模式很统一：每个类是 `Object` 的具体子类，构造函数里通过
`Runtime::type_xxx()` 拿到自己该有的类型对象，`visit_own_refs()` 报出自己持有的其他 SL 对象
（没有就报空）。

| 类               | 存的是什么                                 | 备注                                                                                      |
| ---------------- | ------------------------------------------ | ----------------------------------------------------------------------------------------- |
| `Int`            | 一个 `BigInt`（现成的大数库）              | 不引用别的 SL 对象                                                                        |
| `Decimal`        | 一个 `BigDec`                              | 同上                                                                                      |
| `Str`            | `u32string`（按码点存，不是 UTF-8 字节流） | 按码点取长度/下标是 O(1)，代价是进出要编解码                                              |
| `Tuple`          | `vector<ObjectRef>`                        | **不可变的是这些引用关系本身**，元素指向的对象可以是可变的                                |
| `NamedSingleton` | 一个显示名字符串                           | `None`/`Ellipsis`/`NotImplemented`/`StopIteration` 四个共用这一个类，靠 `type()` 区分身份 |
| `Bool`           | 一个 `bool`                                | `True`/`False` 只有唯一两个实例（`Runtime::singleton_bool()` 保证不会有第三个）           |
| `BaseException`  | `Ref<Tuple>`（叫 `args_`）                 | 整棵异常类树共用这一个 C++ 类，名字取自树根而非 SL 的 `Exception`，见下                   |

**`BaseException` 值得多说两句**：为什么 `TypeError`、`ValueError`、`SyntaxError` ……十几个不同的
SL 异常类，只对应一个 C++ 类？因为它们在存储层面完全一样——都只是"构造时收到的参数元组"，
区别只在 `type()` 是哪一个。这跟 CPython 的做法一致（`BaseException.args` 是它 C 结构体里的
一个槽位，不是走 `__dict__`）。现在 `.args` 只能从 C++ 侧直接读（`BaseException::args()`），
还没法从 SL 代码里通过属性访问（`e.args`）拿到——因为通用的"对象属性表"机制还没实现，这是留给
以后的坑，不是被遗忘的疏漏。

---

## 五、异常怎么从"宿主 C++ 出错"变成"SL 异常对象"

这是这次的新内容，梳理一下几个概念不要混。

### 两层异常，不要搞混

- **`cpp_exceptions/` 里的异常**（`SyntaxError`、`InternalError`、`EncodingError`、
  `FileNotFoundError`）：这是宿主层，纯 C++ 的 `std::exception` 体系，词法/语法分析这些还没有
  SL 对象模型可用的阶段抛的都是这些。它们现在都改成了"存结构化字段"（文件名/行/列/消息分开存），
  而不是抛出时就拼成一整条字符串——因为这些异常最终要被翻译成 SL 对象，翻译的时候需要拿到
  分开的字段，而不是回头去正则解析一条拼好的字符串。
- **`runtime/x_builtin_types.inc` 里的异常类**（`TypeError`、`SyntaxError` 等）：这是 SL 层，
  货真价实的 `Type` 对象，SL 代码里 `except (TypeError)` 抓的就是这些。

**两边都有一个叫 `SyntaxError` 的东西，这是故意的、也是危险的**——宿主层的 `SyntaxError` 是
C++ 类，SL 层的 `SyntaxError` 是一个 `Type*`，千万别看名字搞混。

**`InternalError` 只存在于宿主层，永远不会有对应的 SL 类**——它的语义是"解释器自己实现有 bug"，
如果 SL 代码能捕获它，就等于允许用户代码去 try/except 一个解释器内部错误，这是不希望发生的。

### `HostErrorConversion.h/.cpp`：把宿主异常翻成 SL 对象

三个函数，分别把 `SyntaxError`/`EncodingError`/`FileNotFoundError` 转成对应的 SL `Exception`
对象（塞进 `.args` 的是干净的错误信息，不带文件名/行列号那层格式化前缀）。**`InternalError`
故意没有对应的转换函数**，跟上一段呼应。

这几个函数现在**还没有任何调用方**——因为真正会用到它们的地方（`eval` 求值、加载文件出错）
还没写。提前写好是因为"宿主异常该怎么变成 SL 异常"是一个设计决定（该塞什么字段、对应哪个 SL
类），值得先钉死、单独验证，不想等到虚拟机写到一半时临时决定。

### `RaisedException.h`：`raise` 用的"信封"

这是个很容易被误解的东西，特别说明一下：**它不是"SL 异常怎么在多个 SL 函数调用之间传播"的
机制**。真正的传播机制以后要在虚拟机主循环里手写（因为 SL 的调用帧是堆对象，不是 C++ 的函数
调用栈，C++ 原生的异常展开机制够不到"另一个 SL 帧"）。

`RaisedException` 的使用场景很窄：某个**不会回调 SL 代码**的、纯 C++ 实现的内置操作
（比如以后 `int('abc')` 这种转换失败），想要表达"这一步的结果是抛出一个异常"，就把
SL 异常对象包进这个信封 `throw` 出去，由**紧挨着它的调用方**立刻 `catch` 住，翻译成虚拟机
认识的"这一步产生了异常"这个结果。它的生命周期严格限制在一小段 C++ 调用里，绝不允许穿过 SL 帧
的边界。如果哪里漏接了，说明是代码写错了地方，不是设计允许的用法。

---

## 六、几个横切的设计原则（贯穿以上所有部分）

1. **"只给 X 用"用访问控制表达，不靠注释**：GC 的记账字段、`visit_refs`、`set_type`、
   `incref/decref`、`Heap::link/unlink`、以及各对象类型的构造函数，全是 `private` + 精确的
   `friend`，外部代码碰它们是编译错误而不是"请自觉"。
   `Object::visit_own_refs` 更进一步做成私有虚函数（子类照常覆写，但谁都不能绕过 `visit_refs`
   直接调它——绕过去会静默漏掉 `type_` 那条边）。唯一公开的例外是**只读**的 `refcount()`，
   留给测试断言引用计数收支平衡。
2. **纯虚函数逼着新代码补齐该做的事**：`Object::visit_own_refs()` 是纯虚的——新增一个对象类型，
   忘了实现这个函数会直接编译不过。但要注意这只能保证"每种类型都写了这个函数"，保证不了"写对了、
   没漏字段"——漏报一个字段编译器发现不了，只能靠人仔细看。
3. **状态检查要拦真实存在的外部场景，不要拦自己内部绕出去又绕回来的调用**：`Runtime` 早先版本的
   `BootPhase` 就是反例（见上面「三」那一节），加检查之前先列出所有会读这个状态的调用点，确认
   它们不是自己写的、还绕得回去的内部实现。
4. **能不做的优化现在不做**：GC 不分代、不做写屏障；`collect()` 的触发阈值是个先能跑起来的粗糙
   数字。都是等虚拟机真正跑起来、能测出瓶颈在哪之后再回来处理，现在做纯属基于猜测的过度设计。
5. **裸指针 = 借用，`Ref<T>` = 所有权**，这条规则在整个 `runtime/` 里没有例外（`Type::mro_`
   看着像例外，但上面解释过它为什么可以安全地用裸指针）。看到一个裸指针字段，默认它不需要为
   对象的生死负责；看到一个 `Ref<T>` 字段，默认它是这条引用链条上的一环。

---

## 读代码建议顺序

如果你想对着代码逐文件过一遍，建议这个顺序（跟依赖关系一致，后面的建立在前面的理解之上）：

1. `Object.h`（`Object`、`Ref<T>`、`RefVisitor` 三个概念）
2. `Type.h` / `Type.cpp`
3. `Heap.h` / `Heap.cpp`（这时候 `Object`/`Type` 已经懂了，`collect()` 的五步会比较好懂）
4. `Runtime.h` / `Runtime.cpp`（重点看 `init()` 那两步为什么直接读私有字段、不经过公开访问器）
5. `x_builtin_types.inc`（一分钟就能看完，但建议对着 SL.md 4.5/4.2.23 那两张继承图核对一遍）
6. `objects/` 目录下随便挑几个看，模式都一样，看懂一个基本等于看懂全部
7. `cpp_exceptions/`（几个异常类的字段设计）
8. `HostErrorConversion.h/.cpp`、`RaisedException.h`（最后看，因为它们依赖前面全部概念）
